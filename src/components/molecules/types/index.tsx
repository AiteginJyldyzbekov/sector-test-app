export interface TableRowProps {
  id: number;
  title: string;
  body: string;
}

export interface TableRowProps {
  id: number;
  title: string;
  body: string;
}

export interface PaginationProps {
  totalPosts: number;
  postsPerPage: number;
}
